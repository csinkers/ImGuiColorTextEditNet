using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace TextEdit.Tests;

[TestClass]
public class SelectionTests
{
    // control.HasSelection;
    // control.GetSelectedText();
    // control.GetCurrentLineText();
    // control.SelectAll();
    // control.SelectWordUnderCursor();
    // control.SetSelection();
    // control.SetSelectionStart();
    // control.SetSelectionEnd();
}